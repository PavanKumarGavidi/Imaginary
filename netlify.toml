# Netlify configuration for the IMAGINE studio site
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so deep links / refreshes never 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
